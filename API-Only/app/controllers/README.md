# Controllers

HTTP controllers for backend endpoints.

- `api/v2` contains versioned REST controllers
- `graphql_controller.rb` handles GraphQL requests
- `application_controller.rb` defines shared API behavior
