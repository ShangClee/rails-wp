# Models

Domain models mapped to the WordPress-compatible schema.

- `wp_*` models map legacy WordPress tables
- model concerns contain shared domain behavior
- keep persistence rules in models and workflows in services
