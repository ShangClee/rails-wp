Develop a production-ready headless CMS reference implementation using Ruby on Rails 8 with minimal dependencies, strictly based on the WordPress v6.9.1 database schema defined in `/Users/shang/Prj2026/rails-wp/WordPressInitDBv.6.9.1.sql` . The implementation must include: (1) Complete analysis and ORM mapping of all WordPress core tables (wp_posts, wp_postmeta, wp_users, wp_usermeta, wp_terms, wp_term_taxonomy, wp_term_relationships, wp_options, wp_comments, wp_commentmeta, wp_links) to Rails ActiveRecord models with proper associations, validations, and database constraints that maintain referential integrity. (2) Full RESTful API v2 endpoints that replicate WordPress functionality including posts/pages/custom post types CRUD operations with proper filtering, pagination, and search capabilities; complete user management with registration, profile updates, and password reset; media library handling with file upload, metadata extraction, and image resizing; taxonomy management for categories and tags with hierarchical support; custom fields (postmeta) integration with JSON serialization. (3) Authentication system implementing WordPress user roles (Administrator, Editor, Author, Contributor, Subscriber) and capabilities using Rails authentication with JWT tokens, session management, and role-based access control that mirrors WordPress permission checks. (4) GraphQL schema using graphql-ruby gem defining types for all WordPress entities with proper relationships, implementing queries for posts, users, terms, and media with filtering arguments, mutations for content management, and subscriptions for real-time updates. (5) Multi-layer data validation and sanitization implementing WordPress security standards including SQL injection prevention, XSS protection, CSRF tokens, input sanitization matching WordPress functions (sanitize_text_field, esc_html, esc_sql), and output escaping for all API responses. (6) Multi-level caching architecture using Rails cache with Redis for database query results, fragment caching for API responses, HTTP caching headers (ETag, Last-Modified), and cache invalidation strategies triggered by content updates. (7) Migration utilities as Rails rake tasks to export existing WordPress content including full site export with media files, selective content migration by date/type/author, JSON/CSV export formats, and import validation with rollback capabilities. (8) Comprehensive documentation including OpenAPI 3.0 specification for REST endpoints, interactive GraphQL playground with examples, database ERD diagrams showing all WordPress relationships, integration examples for React/Next.js/Vue.js frontends, deployment guides for Docker/Kubernetes, and performance optimization guidelines. All database queries must use ActiveRecord optimizations (includes, joins, select) with proper indexing strategies, implement database views for complex queries, maintain WordPress database integrity constraints, and include query performance monitoring with slow query logging.